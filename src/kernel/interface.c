#include <interface.h>
#include <scheduler.h>
#include <timer.h>

void init_shell(void) {
    // Initialisation du shell
}

void execute_command(const char* command) {
    // Commandes basiques
    (void)command;
}